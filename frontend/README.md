# KIIT Eats Multi-Module React App

## Run

```bash
npm install
npm run dev
```

## Dummy Credentials

- User: `user` / `user123`
- Vendor: `vendor` / `vendor123`
- Admin: `admin` / `admin123`

The provided user frontend UI has been integrated under `src/user`.
